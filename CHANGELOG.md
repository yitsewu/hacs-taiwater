# 更新紀錄

## 0.4.2

- 修正英數混合水號的帳單詳細按鈕無法辨識、誤報網站格式變更的問題。
- 解析規則與設定表單一致，維持 11 碼 ASCII 大寫英數字及詳細資料水號一致性檢查。
- 新增純數字／英數水號、錯戶拒絕及異常按鈕的回歸測試。

## 0.4.1

- 新增 HACS 官方驗證與台灣地區設定，準備申請預設目錄收錄。
- 統一新儲存庫 `yitsewu/hacs-taiwater` 的文件與套件連結。
- 簡化帳戶設定文字與 README；沿用內建 OCR、帳單歷史及既有帳戶設定。


## 0.4.0

- OCR 改為整合內建 NumPy 推論及 MIT 模型，HA requirements 使用與 Core 2026.9.1 相同的 NumPy／Pillow。
- 不需 OCR 附加元件、ONNX Runtime、OpenCV 或下載模型；既有外部 OCR 僅作進階選用備援。
- 模型 SHA-256 驗證、executor 載入及推論、跨帳戶共享／串行、圖片限制與失敗回復。
- 保留人工驗證與分離的 OCR／驗證／查詢紀錄，新增 OCR 執行方式。
- 保留既有等額分攤預設、總用水量名稱、品牌圖示及 0.3.0 背景統計優化；既有帳戶不需重填。
- 新增真正官方 Core amd64／aarch64 及 QEMU 舊 CPU 基線測試；附中英文安裝與模型來源說明。

## 0.3.0

- 帳單查詢與長期統計寫入分離；保存帳單及查詢紀錄後立即完成查詢，不再因 Recorder 匯入占用查詢鎖。
- 背景工作串行寫入，同時到達的更正合併為最新版本；保留完整替換及讀回驗證，避免更正日期後殘留舊資料。
- 分攤及逐時序列計算移到 executor。相同分攤在同次載入期間略過重複寫入。
- 查詢中暫停查詢按鈕；同時呼叫動作時使用專屬忙碌提示，不誤記查詢失敗。
- 新增「重建長期統計」按鈕、統計完成時間與耗時感測器，以及等待／寫入中等中英文狀態。
- 統計失敗可從保存帳單重試，不連線台水、不執行 OCR、不更改查詢紀錄。解除載入等待背景工作結束。
- ZIP 使用固定時間、權限與文字換行，確保相同原始碼得到相同 SHA-256。
- 沿用既有儲存格式及 OCR 0.1.1；不需重新填帳戶資料。

## 0.2.2

- 僅對官方網站憑證鏈缺少 Subject Key Identifier 的錯誤加入一次 TLS 相容重試；保留信任及 hostname 驗證。

## 0.2.1

- OCR 0.1.1 固定 NumPy 2.3.5，支援較舊的 HAOS 虛擬 CPU 指令集。

## 0.2.0

- 新增最近 100 次查詢紀錄與指定帳期查詢動作；區分 OCR、原站驗證及查詢結果。
